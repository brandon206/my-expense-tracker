import { extendTheme } from "@chakra-ui/react";

const metTheme = extendTheme({
  styles: {
    global: {
      body: {
        bg: "#061423",
        color: "white",
      },
    },
  },
});

export default metTheme;